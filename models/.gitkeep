# SAM model checkpoints are downloaded here automatically on first run.
# Files are large (up to ~2.5 GB). Do not commit to version control.
# Supported variants:
#   sam_vit_h_4b8939.pth  (~2.5 GB) — highest accuracy
#   sam_vit_l_0b3195.pth  (~1.2 GB) — balanced
#   sam_vit_b_01ec64.pth  (~375 MB) — fastest
